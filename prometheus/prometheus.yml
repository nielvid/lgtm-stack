global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s
  external_labels:
    cluster: lgtm-stack
    environment: production
    team: anvila
    service: anvila-api

# ---- Alertmanager ------------------------------------------
alerting:
  alertmanagers:
    - static_configs:
        - targets: ['localhost:9093']

# ---- Alert Rules -------------------------------------------
rule_files:
  - /etc/prometheus/rules/*.yml

# ---- Scrape Configs ----------------------------------------
scrape_configs:

  # Prometheus self-monitoring
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']

  # Node Exporter — host system metrics (CPU, RAM, Disk, Network)
  - job_name: node_exporter
    scrape_interval: 15s
    static_configs:
      - targets: ['localhost:9100']
        labels:
          instance: lgtm-host

  # Blackbox Exporter — HTTP, SSL probes
  - job_name: blackbox_http
    metrics_path: /probe
    params:
      module: [http_2xx]
    static_configs:
      - targets:
          - https://example.com         # Replace with your actual endpoints
          - http://localhost:8080/health
          # - ${STAGING_URL}
          # - ${PRODUCTION_URL}
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: localhost:9115

  # Blackbox Exporter — SSL certificate expiry check
  - job_name: blackbox_ssl
    metrics_path: /probe
    params:
      module: [tcp_connect]
    static_configs:
      - targets:
          - example.com:443
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: localhost:9115

  # Alertmanager self-monitoring
  - job_name: alertmanager
    static_configs:
      - targets: ['localhost:9093']

  # Grafana self-monitoring
  - job_name: grafana
    static_configs:
      - targets: ['localhost:3000']

  # Loki self-monitoring
  - job_name: loki
    static_configs:
      - targets: ['localhost:3100']

  # Tempo self-monitoring
  - job_name: tempo
    static_configs:
      - targets: ['localhost:3200']

  # OpenTelemetry Collector self-metrics
  - job_name: otel_collector
    static_configs:
      - targets: ['localhost:8889']

  # Prometheus Pushgateway — DORA metrics from GitHub Actions
  - job_name: pushgateway
    honor_labels: true
    static_configs:
      - targets: ['localhost:9091']

  # Demo instrumented application
  - job_name: demo_app
    scrape_interval: 15s
    static_configs:
      - targets: ['localhost:8080']
        labels:
          host: demo-host
          service: demo-app
          environment: production
